What is the difference between == and = in Python;== is used for comparison (checks if two values are equal), while = is used for variable assignment.brbrPT-BR == é usado para comparação (checa se dois valores são iguais), enquanto = é usado para atribuição de variável.
How does Python compare strings using comparison operators like  or ;It compares them alphabetically (lexicographically). For example, James  Brian is True because J comes after B.brbrPT-BR Ele as compara em ordem alfabética. Por exemplo, James  Brian é True porque J vem depois de B.
What is the purpose of the elif keyword;It stands for else if and is used to test a new condition only if the previous if statement was False.brbrPT-BR Significa senão se e serve para testar uma nova condição apenas se o if anterior tiver sido Falso.
What are the two crucial formatting rules for Python conditionals and loops;1) End the conditionalloop line with a colon (); 2) Indent the nested block of code using Tab or 4 spaces.brbrPT-BR 1) Terminar a linha da condicionalloop com dois pontos (); 2) Indentar o bloco de código interno usando Tab ou 4 espaços.
In for item in shopping_list, what is the iterator and what is the iterable;item is the iterator (temporary variable for the current element), and shopping_list is the iterable (the collection being looped through).brbrPT-BR item é o iterador (variável temporária do elemento atual) e shopping_list é o iterável (a coleção que está sendo percorrida).
Which dictionary method should you use to loop through both keys and values at the same time;The .items() method (e.g., for key, value in my_dict.items()).brbrPT-BR O método .items() (ex for key, value in meu_dicionario.items()).
What specific numbers are generated by range(1, 6);Numbers from 1 to 5. The stop value (6) is always excluded.brbrPT-BR Números de 1 a 5. O valor de parada (6) é sempre excluído.
What causes an infinite loop in a while loop, and how do you stop it in the terminal;It happens when the loop's condition never becomes False. To stop it, press Ctrl + C (Windows) or Cmd + C (Mac).brbrPT-BR Acontece quando a condição do loop nunca se torna Falsa. Para parar, pressione Ctrl + C (Windows) ou Cmd + C (Mac).
What does the break keyword do inside a loop;It immediately terminates and exits the loop, even if the loop's main condition is still True.brbrPT-BR Interrompe e sai do loop imediatamente, mesmo se a condição principal do loop ainda for Verdadeira.
What is the difference between the logical operators and and or;and requires ALL conditions to be True to pass. or requires at least ONE condition to be True.brbrPT-BR and exige que TODAS as condições sejam Verdadeiras. or exige que pelo menos UMA condição seja Verdadeira.
How do you check if an item does NOT exist inside a list or dictionary without looping;By using the not in membership operator.brbrPT-BR Usando o operador de associação not in.
How do you add a new element to the end of an existing list in Python;By using the .append() method (e.g., list_name.append(item)).brbrPT-BR Usando o método .append() (ex nome_da_lista.append(item)).